rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all collections in Bimbel Sigma app
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
